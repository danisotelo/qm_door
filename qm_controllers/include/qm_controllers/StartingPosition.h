// StartingPosition.h
#ifndef STARTINGPOSITION_H
#define STARTINGPOSITION_H

#include <cmath>

namespace qm {

    constexpr double X = -2;
    constexpr double Y = 0; // 1
    constexpr double PSI_DEG = 0; // -40
    constexpr double PSI = PSI_DEG * (M_PI / 180);
    constexpr double ARM_DIST = 0.6;
    constexpr double HEIGHT = 0.4;
    constexpr double ARM_HEIGHT = 0.036;
    constexpr double THRESHOLD = 0.1;

} // namespace qm

#endif // CONSTANTS_H